import React from 'react';

function DeleteButton(props) {
    if(user.id === _id){
        return <button>x</button>
    }
    return (
        <div>
            
        </div>
    );
}

export default DeleteButton;