import React from 'react';

function Navbar(props) {
    return (
        <div className="navbar">
            <nav>
                <span>Add</span>
                <span>Read</span>
                <span>Unread</span>
          </nav>
        </div>
    );
}

export default Navbar;